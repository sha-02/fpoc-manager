{% if multicast %}

config system settings
    set multicast-forward disable
    set multicast-ttl-notchange enable
    set gui-multicast-policy enable
end

config firewall multicast-policy
    edit 1
        set name "permit all"
        set srcintf "any"
        set dstintf "any"
        set srcaddr "all"
        set dstaddr "all"
    next
end

{% set tunnels = [ 'H1_INET1', 'H1_INET2', 'H1_MPLS', 'H2_INET1', 'H2_INET2', 'H2_MPLS' ] %}

{% if full_mesh_ipsec %} {# Add cross-underlay IPsec tunnels #}
    {% set tunnels = tunnels + [ 'H1_INET1X2', 'H1_INET2X1', 'H2_INET1X2', 'H2_INET2X1' ] %}
{% endif %}

config router multicast
    set multicast-routing enable
    config pim-sm-global
        # Control local-out PIM with an SD-WAN rule
        set pim-use-sdwan enable
    end
    config interface
        {% for tunnel in tunnels %}
            edit "{{tunnel}}"
                set pim-mode sparse-mode
                {{ "set update-source lo-BGP" if overlay == 'no_ip' }}
            next
        {% endfor %}
    end
end

# LANs are limited to accept IGMP join only for specific groups

{% if not vrf_segmentation %} {# ################### No VRF segmentation ############# #}
config router multicast
    {# Rendez-vous points #}
    {% if vrf_pe == 0 %}
        # "pim-sm-global" is used for RPs in VRF 0
        config pim-sm-global
    {% else %} {# vrf_pe<>0 #}
        # "pim-sm-global-vrf" is used for RPs in VRF <> 0
        config pim-sm-global-vrf
        edit {{vrf_pe}}
    {% endif %} {# vrf_pe == 0 #}
        config rp-address
            edit 1
                set ip-address {{rendezvous_points['WEST-DC1']}}
                set group "multicast-west-dc1"
            next
            edit 2
                set ip-address {{rendezvous_points['WEST-DC2']}}
                set group "multicast-west-dc2"
            next
            edit 3
                set ip-address {{rendezvous_points['EAST-DC1']}}
                set group "multicast-east-dc1"
            next
        end
    end
    {% if vrf_pe != 0 %}
    end
    {% endif %} {# vrf_pe<>0 #} {# end of RDV points #}

    config interface
        edit "{{lan.port}}"
            set pim-mode sparse-mode
            set passive enable
            config igmp
                set access-group "multicast-groups"
            end
        next
    end
end
{% endif %} {# no vrf_segmentation #}


{% if vrf_segmentation %} {# ################### With VRF segmentation ############# #}
config router multicast
    config pim-sm-global-vrf
        edit {{vrf_blue}}
            config rp-address
                edit 1
                    set ip-address {{rendezvous_points['WEST-DC1']}}
                    set group "multicast-blue"
                next
            end
        next
        edit {{vrf_yellow}}
            config rp-address
                edit 1
                    set ip-address {{rendezvous_points['WEST-DC2']}}
                    set group "multicast-yellow"
                next
            end
        next
        edit {{vrf_red}}
            config rp-address
                edit 1
                    set ip-address {{rendezvous_points['EAST-DC1']}}
                    set group "multicast-red"
                next
            end
        next
    end

    config interface
        {% for vrf_name, segment in vrf_segments.items() %}
            edit "{{segment.name}}"
                set pim-mode sparse-mode
                set passive enable
                config igmp
                    set access-group "multicast-{{vrf_name|lower}}"
                end
            next
        {% endfor %}
    end
end
{% endif %} {# vrf_segmentation #}

{% endif %} {# multicast #}