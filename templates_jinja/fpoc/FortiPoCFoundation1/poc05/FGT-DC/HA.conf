{% if HA == 'FGCP' %}
# HA FGCP
# Additional HA FGCP settings which are not set by the bootstrap configuration

# config system ha
#     set group-id {{i}}
#     set group-name "HA-{{i}}"
# end
{% endif %}