{% if FMG_FORTIGATE_ID %}
    {% import 'SETTINGS.DEVICES' as SETTINGS with context %}

    {% set name = SETTINGS.DEVICES[FMG_FORTIGATE_ID].name %}
{% endif %}

###############################################################################################################

config system global
    set hostname {{name}}
    set gui-theme neutrino
end

